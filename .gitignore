# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.tsbuildinfo

# env / secrets
.env
.env*.local
.env.local

# Claude Code local settings — holds a machine-local permission allowlist that
# includes psql commands with the DB password. Must never be tracked.
.claude/settings.json
.claude/settings.local.json

# vercel
.vercel

# test
/coverage
